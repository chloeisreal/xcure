{
  "name": "cure-contracts",
  "version": "1.0.0",
  "description": "Cure Token contracts for L3 POC",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy-cure.ts --network arbitrumSepolia",
    "deploy:local": "hardhat run scripts/deploy-cure.ts --network local",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
