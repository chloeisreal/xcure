# Anthropic API key — when set, the /api/analyze route uses the real Claude API.
# When absent, the route falls back to a mock streamed response.
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...
